# 제출 요건

> 한국어 번역본입니다. 정본은 [SUBMISSION.md](SUBMISSION.md)이며, 두 본이 어긋날
> 경우 영어 본문이 우선합니다.

아래 리포트들이 곧 제출물이고, **제출물은 커밋 하나로 식별됩니다. 커밋하십시오.**
`reports/`는 의도적으로 무시 목록에서 제외했습니다. Git에 들어가면 안 되는 것은
「소스와 실행」에 적힌 대로이며 **위치가 아니라 내용의 문제**입니다 — 비밀값, 데이터셋
원본, 생성된 인덱스, 제한된 산출물. 그런 것을 담게 되는 대량 실행 덤프는 무시되는
디렉터리에 두고, **그것을 인용하는 리포트는 커밋에 두십시오.**

## 소스와 실행

- 전체 소스와 의존성 잠금 파일.
- 비대화형(non-interactive) 데이터셋 검증·인덱스 구축·평가·데모 실행 명령.
- 클린 체크아웃 기준 셋업 안내.
- 단위 테스트, 통합 테스트, 컨트랙트 테스트.
- 비밀값, 데이터셋 원본, 생성된 인덱스, 제한된 산출물을 포함하지 않을 것.

## 아키텍처 리포트

다음을 문서화합니다.

- 레코드 선택과 문서 종류에 대한 결정
- 정규화, 청킹, 결정론적 식별자 규칙
- 인덱스 매핑, 검색, 융합, 필터링, 리랭킹
- 임베딩 및 LLM 어댑터 경계
- 근거 부여(grounding), 인용, 거부, 실패 동작
- 이식성 경계와, 알려진 Peitho 적응 작업
- 의존성, 라이선스, 보안 가정, 운영상 한계

## 검색 평가 리포트

다음을 공개합니다.

- 테스트 질의의 출처와 구성 방법
- 답변 가능·답변 불가·범위 밖 질의의 구성 비율
- 관련성 라벨링 방법과, 복수 정답 처리 방식
- 출처 파생 질의를 포함한 누출(leakage) 통제 방안
- 정확한 지표 정의와 컷오프
- 전체 집계 결과 및 도메인별 결과
- 실패한 질의에 대한 분석
- 지연 시간 백분위수, 인덱스 크기, 인덱스 구축 시간, 재구축 횟수
- 데이터셋, 정규화, 청킹, 임베딩, 인덱스 버전

## 생성 평가 리포트

근거 부여, 인용 정합성, 뒷받침되지 않는 인용/환각, insufficient-evidence 거부,
out-of-scope 거부, 지연 시간, 토큰 사용량, 비용을 보고합니다. **결정론적인 검색
측정과, 확률적이라 반복 측정이 필요한 생성 측정을 분리하십시오.**

## 작업 리포트

- 최초 추정치와 마일스톤 계획.
- 마일스톤별 실제 소요 시간.
- 접근 실패와 환경 문제를 포함한 블로커 로그.
- 완료한 작업, 미완료 작업, 의도적으로 보류한 작업.
- AWS 사용량: 임베딩 호출 수, 생성 호출 수, 입력/출력 토큰, OpenSearch 사용량,
  재구축 횟수, 추정 비용. **기여자들이 IAM 사용자 하나를 공유하므로 청구서도
  CloudTrail도 이 값을 여러분에게 귀속시키지 못합니다.** 직접 작성한 계측에서만,
  첫 호출부터 기록해야 나오는 값이며 **사후 복원이 불가능합니다.**
- 일주일이 더 주어진다면 무엇을 할 것인가.

MZO는 개인 머신 상태, IDE 상태, 노트북(notebook), 목록에 없는 외부 서비스 없이
인덱스를 재구축하고 보고된 평가를 재실행할 수 있어야 합니다.
