# 소스 레코드 스키마

> 한국어 번역본입니다. 정본은 [DATASET_SCHEMA.md](DATASET_SCHEMA.md)이며, 두 본이
> 어긋날 경우 영어 본문이 우선합니다.

[DATASET.md](DATASET.md)는 릴리스가 어떻게 전달되고 무엇을 할 수 있는지를 규정합니다.
이 문서는 **전달되는 레코드 하나가 어떻게 생겼는지**를 설명하여, 릴리스가 도착하기
전에 정규화와 청킹을 설계할 수 있도록 합니다.

계약의 실체는 `src/legal_agent_assessment/dataset.py`의 Pydantic 모델과
`src/legal_agent_assessment/dataset_validation.py`의 결정론적 검사입니다. 이 산문은
그것을 설명할 뿐이며, 둘이 어긋나면 **모델이 계약이고, 서명된 릴리스 매니페스트가
그 둘보다 우선합니다.**

## 이 문서가 정하는 것과 열어 두는 것

MZO는 **입력** 측을 고정합니다. 출처 신원, 출처 이력(provenance), 인용 좌표,
반입 허용(admission), 사용 처분(usage disposition), 릴리스 무결성입니다. 이것을
고정해야 비교가 「이질적인 파일을 파싱하는 연습」으로 변질되지 않고, MZO가 모든
제출물을 재실행할 수 있습니다.

그 아래로는 아무것도 고정하지 않습니다. 제공된 범위 안에서의 레코드 선택, 정규화,
청킹, 인덱스 매핑, 검색, 테스트셋, 관련성 판정, 지표, 프롬프트는
[ASSIGNMENT.md](ASSIGNMENT.md)가 밝힌 대로 **기여자의 것**입니다. 색인되는 모든
청크는 소스 레코드 정확히 하나로 추적되어야 하지만, 그 청크의 식별자·순번·경계·
메타데이터 형태는 이 스키마의 필드가 아니라 **기여자의 산출물**입니다.

이 문서에 건수는 등장하지 않습니다. 모든 수치는 릴리스 매니페스트가 정본입니다.

## 형식과 표기

레코드는 JSONL이며, 한 줄에 객체 하나, UTF-8입니다. 필드명은 영문 `camelCase`이고,
수집기가 경계에서 원본 한글 키를 매핑합니다. 파이썬 속성은 `snake_case`이므로,
`model_dump(by_alias=True)`가 전송 형식을 만들고 `model_validate`가 그것을 받습니다.

**모르는 필드는 거부합니다.** 예상에 없던 필드는 우회할 대상이 아니라 보고할
전달 문제입니다.

## 레코드

종류와 무관하게 모든 레코드가 갖는 필드입니다.

| 필드 | 의미 |
| --- | --- |
| `documentId` | 전달된 문서 하나의 안정적·유일한 신원 |
| `sourceGroupId` | 분할 안전(split safety). 같은 판결인 레코드들이 하나를 공유함 |
| `documentKind` | `judgement` 또는 `official_guide` |
| `title` | 사람이 읽는 이름 |
| `text` | 전달된 그대로의 원문 |
| `contentHash` | `text`의 `sha256:<소문자 hex 64자>` |
| `identity` | 인용 좌표. `documentKind`로 구분되는 판별 유니온 |
| `provenance` | 제공처, 발행 표기, URL, 참조, 취득 시각, 선택적 보존 산출물 |
| `admission` | `exempt` · `licensed` · `restricted` |
| `attribution` | `admission`이 `licensed`이면 필수 |
| `usage` | `index_eligible` 또는 `evaluation_only` |
| `inDefaultCorpus` | MZO 기준 코퍼스에 속하는지 여부 |
| `linkedLaws` | 대상 법령과 이 레코드가 각 법령에 대해 갖는 관계 |
| `limitations` | 알려진 결손. 고치지 않고 명시함 |

**레코드 하나는 소스 문서 하나 전체입니다.** 108쪽짜리 가이드도 전문을 담은 레코드
하나로 도착합니다. 이를 인용 단위로 쪼개는 것이 청킹입니다.

`rawArtifactPath`와 `rawArtifactHash`는 선택이며 **둘 다 있거나 둘 다 없습니다.**
응답 바이트를 보존하는 결정은 텍스트의 출처를 기록하는 결정과 별개입니다. 해시 없는
경로는 「산출물을 보존했다」고 읽히면서 검증이 불가능합니다.

### 허가와 범위는 서로 다른 질문입니다

`usage`는 **허가이며 단단한 경계**입니다. `evaluation_only` 자료는 어떤 경우에도
인덱스에 도달해서는 안 되며, 기여자가 무엇을 결정하든 이는 바뀌지 않습니다.

`inDefaultCorpus`는 **범위**입니다. 일부 레코드는 MZO의 기준 범위 밖에 있으면서도
의도적으로 전달됩니다. 대상 법령 본문 검색에 걸려 들어온 것들로, 인용 가능하고 색인
자격도 있지만 주제가 이 에이전트가 답하려는 질문에서 비켜나 있습니다. MZO의 기본값은
이를 빼는 것입니다.

**포함해도 됩니다.** 기본 범위 밖의 전달된 레코드까지 코퍼스를 넓히는 것은 통상적인
레코드 선택이며, [DATASET.md](DATASET.md)가 이미 허용 변환으로 열거하고 있습니다.
허용되는 이유는 그 경계가 사실이 아니라 **판단**이기 때문입니다. 어느 쪽을 택하든
결정을 설명하십시오. 이것은 「코퍼스 데이터 추가」가 아닙니다 —
[ASSIGNMENT.md](ASSIGNMENT.md)의 금지는 **릴리스 바깥의 자료**를 두고 하는 말입니다.

`default_corpus(records)`가 기준 범위를 주고, `select_index_inputs(records)`가 색인
가능 여부를 결정하며 **`inDefaultCorpus`를 참조하지 않습니다.** 기본 코퍼스 밖에
있으면서 색인에 아무 문제가 없는 레코드가 있을 수 있고, `evaluation_only` 레코드는
둘 다 아닙니다.

### 판례 신원(judgement identity)

`caseSerial`, `caseName`, `caseNumber`, `court`, `caseCategory`,
`decidedOn`(`YYYYMMDD`), `judgementType`, 그리고 요약·상호참조 4개 필드
`headnote`, `holding`, `referencedProvisions`, `referencedPrecedents`.

**키는 `caseSerial`입니다. `caseNumber`가 아닙니다.** 그런데 규칙보다 이유가 더
중요합니다.

사건번호는 반복되며, 반복되는 곳에서는 **법원과 선고일자까지 같습니다.** 이는 다른
판결이 같은 표기를 재사용한 것이 아니라, **같은 판결이 서로 다른 일련번호로 두 번
수록된 것**입니다. 본문이 바이트 단위로 동일한 쌍도 있고, 나머지도 근소하게만
다릅니다.

따라서 이것은 장부 정리 문제가 아니라 **분할 안전(split safety) 문제**입니다. 한 쌍
중 하나를 테스트셋에 넣고 다른 하나를 색인하면, 검색기는 **정답의 근사 복사본**을
찾아내고 실행 결과는 그것을 재현율로 기록합니다 — [ASSIGNMENT.md](ASSIGNMENT.md)가
실제 사용자 품질로 제시하는 것을 금지한 바로 그 상황입니다. `caseSerial`로 중복
제거를 해도 소용없습니다. **두 레코드의 일련번호가 다르다는 것이 애초에 둘 다 들어온
이유이기 때문입니다.**

같은 판결인 레코드들은 같은 `sourceGroupId`를 갖습니다. 어떤 분할을 하든 한 그룹은
한쪽에 통째로 두십시오. validator는 아직 그룹을 공유하지 않는 중복 판결을
`ungrouped_duplicate_decision`으로, 이미 공유하는 것을 `duplicate_decision`으로
보고합니다. 후자는 결함이 아니라 **분할 방식에 걸리는 제약**입니다.

### 가이드 신원(guide identity)

`issuingAuthority`, `issuingDivision`, `officialNumber`, `officialNumberScheme`,
`edition`, `issuedOn`, `pageCount`, `legallyBinding`, `nonBindingStatement`.

가이드에는 사건번호·법원·선고일자가 없습니다. 발행기관과 공식 번호가 있는데,
**승인된 두 가이드는 서로 다른 기관이 서로 다른 체계로 번호를 매깁니다.** 그래서
`officialNumberScheme`이 그 번호가 어느 체계에 속하는지를 말합니다. 이것이 없으면
공식 번호는 등록부로 되짚을 수 없는 문자열일 뿐입니다.

`issuedOn`은 **간기(colophon)가 밝힌 정밀도를 그대로 유지합니다** — `YYYY`,
`YYYY-MM`, `YYYY-MM-DD`. 앞부분에 월까지만 적힌 가이드는 `YYYY-MM`으로 전달됩니다.
일자까지 채우는 것은 없는 출처 사실을 만들어 내는 일입니다.

`legallyBinding`은 `false`로 고정이고, `nonBindingStatement`에는 그 문서가 스스로
그렇게 밝힌 문장이 담깁니다. **가이드는 법령이 아니라 해석입니다.** 별도의 문서
종류로 색인하고 답변에서도 구분해 인용하십시오. 검색이 옳았더라도 가이드를 구속력
있는 것처럼 제시한 답변은 틀린 답변입니다.

## 결측은 세 가지 모습으로 도착합니다

**하루를 날릴 가능성이 가장 큰 함정입니다.** 모든 키가 모든 레코드에 존재하므로
`if "holding" in record:`는 **항상 참**이고 아무것도 알려 주지 않습니다. 부재는 세
방식으로 표현되며, 스스로를 드러내는 것은 첫 번째뿐입니다.

| 모습 | 위치 | 왜 위험한가 |
| --- | --- | --- |
| `""` | 판례 요약·상호참조 4개 필드 | 정직합니다. 참/거짓 검사로 잡힙니다. |
| `decidedOn`의 `"00010101"` | 특정 제공처의 판례 | 8자리이고 `YYYYMMDD`로 **유효합니다**. `strptime`이 성공하여 서기 1년 1월 1일이 됩니다. 「2020년 이후」 필터는 그 레코드들을 **정확하게** 떨어뜨리고, 디버깅할 오류를 남기지 않습니다. |
| `judgementType`의 `"null"` | 같은 레코드들 | null이 아니라 **네 글자 문자열**입니다. 비어 있지 않으므로 공백 검사를 통과하고, 인용문에 `null`이라는 단어로 그대로 출력됩니다. |

릴리스는 세 가지를 **수집된 그대로 보존합니다.** 이를 정규화하는 것은 결과가 따르는
설계 결정이며 — 해당 레코드를 버리거나, 필드를 null로 만들거나, 명시적 unknown을
두는 것 모두 방어 가능합니다 — **그 결정은 기여자의 것입니다.** MZO가 지는 의무는
규약을 발견하게 두지 않고 **명시하는 것**입니다.

`validate_release`는 센티널을 **경고로만** 보고하며 오류로 올리지 않습니다. 경고는
코퍼스를 서술할 뿐, 전달을 막지 않습니다.

## 법령 연계는 1:N이고, 연결되었다고 관련된 것은 아닙니다

`linkedLaws`는 각 대상 법령을 `strength`와 짝지어 담습니다.

- `core` — 그 법령이 해당 판결의 **실제 근거**입니다.
- `candidate` — 관련이 있어 보이나 근거로 확립되지 않았습니다.
- `unlinked` — 본문 검색으로 딸려 왔고, 법령을 **언급만** 할 뿐 그에 관해 판단하지
  않습니다.

한 레코드가 여러 법령에 연결될 수 있습니다. 이 관계를 단일 값으로 모델링한 필터는
다중 법령 레코드를 **조용히** 잘못 처리합니다.

더 중요한 것은 이것입니다. **어떤 법령에 연결된 레코드가 곧 그 법령에 관한 판결인
것은 아닙니다.** 코퍼스의 상당 부분은 `core` 연결이 하나도 없습니다 — 본문 검색에
걸렸을 뿐입니다. `record.core_laws`가 근거를 주고, `linkedLaws`는 매칭을 줍니다.
후자를 전자로 취급하면 그것을 기준으로 측정한 모든 값이 부풀려집니다.

## 제공처별 편차

레코드는 둘 이상의 제공처에서 옵니다. 그리고 **제공처에 따라 필드 충실도가 극단적으로
갈립니다.** 어떤 제공처는 대부분의 레코드에 판시사항과 판결요지를 제공하고, 다른
제공처는 자기 레코드 **어느 하나에도** 둘 다 제공하지 않으면서 위에서 설명한 센티널
날짜와 처분을 함께 실어 보냅니다. 요약 4개 필드가 전부 빈 채로 — 본문만 있는 상태로
— 도착하는 판례도 상당한 비중을 차지합니다.

`provenance.provider`가 어느 제공처에서 왔는지 식별하므로, 충실도·도메인 적합성·
센티널 존재 여부를 추론이 아니라 **필드 하나에서** 알 수 있습니다.
`validate_release`는 본문만 있는 레코드를 `body_only_judgement`로 보고합니다.

**요약 필드가 없을 때에도 정규화가 동작해야 합니다.** 코퍼스의 일부에서는 그것이
항상 없기 때문입니다.

## 본문의 형태

판례 본문은 길고 분포가 고르지 않습니다. 동결 전 후보 집합 기준으로 중앙값은 약
5,000자, 99분위는 100,000자 이상, 최장 레코드는 500,000자를 넘었습니다 — **세 자릿수
규모의 편차**입니다. 여기서 파생되는 무엇이든 꼬리가 지배합니다. 한 고정 청크 크기에서
측정했을 때 상위 10%의 레코드가 전체 청크의 절반가량을 만들어 냈습니다. 이 수치는
**후보 집합을 서술한 것이지 릴리스가 아닙니다.** 매니페스트가 정본입니다.

단락 구조는 리터럴 `<br/>` 마크업이 담고 있으며, 거의 모든 레코드에 존재합니다.
개행 문자는 소수에만 있습니다. **`\n`으로 분할하는 코드는 대부분의 판례를 끊기지 않은
한 줄로 보게 됩니다.**

가이드는 PDF에서 추출한 텍스트로 전달되며 균일하지 않습니다. 추출 품질은 각 PDF가
어떻게 생성되었는지에 좌우되어, 단어 사이 공백이 살아 있는 것과 그렇지 않은 것이
있습니다.

의료광고 가이드의 **위법 사례 상당수는 이미지**여서 텍스트에 전혀 나타나지 않습니다.
이는 단순한 서식 결손보다 무겁습니다. **구체적 사례가 곧 이 가이드를 채택한 이유**이기
때문입니다 — 추상적 요건을 특정 광고 문구로 옮겨 주는 것이 바로 그 사례들입니다.
해당 레코드는 이 결손을 `limitations`에 명시합니다.

MZO는 이미지를 릴리스에 전사해 넣는 대신 **별도의 OCR 솔루션을 제공합니다.** 이를
실행하고 결과를 어떻게 쓸지는 기여자의 몫입니다. 전달받은 문서에서 텍스트를 복원하는
것은 코퍼스 데이터 추가가 아니므로 허용됩니다. 다만 그 산출물은 **기여자가 만든
파생물**이므로, 버전을 매기고, 어느 레코드에서 나왔는지 추적 가능하게 두고, 방법과
오류율을 검색 결과 주장과 함께 기술하십시오. **직접 만든 텍스트와 MZO가 전달한 텍스트는
같은 근거가 아니며**, 인용문이 그 둘을 같은 것처럼 제시해서는 안 됩니다.

MZO는 청크 크기도, 청킹 전략도, 검색 결과도 공표하지 않습니다. **그것을 데이터에서
결정하는 일이 곧 과제입니다.**

## 릴리스 매니페스트

`ReleaseManifest`는 데이터셋 버전과 스키마 버전, 동결 시각, 전달 신원, 각 파일의
SHA-256·바이트 크기·레코드 수, 문서 종류·제공처·사용 처분별 커버리지, 포함/제외
처분과 그 사유, 라이선스 및 제한적 사용 사항, 알려진 결손을 기록합니다.

**이것이 정본입니다.** 어떤 문서·메모·예시의 수치가 이와 어긋나면 매니페스트가
이깁니다.

## 검사

`validate_release(records, manifest)`는 예외를 던지지 않고 findings를 반환하므로,
한 번의 실행으로 모든 문제가 드러납니다.

- **오류(error)는 막습니다.** `documentId` 중복, 전달된 본문과 맞지 않는
  `contentHash`, 매니페스트와 어긋나는 레코드 수나 커버리지. 이런 것은 MZO에
  보고하십시오. **로컬에서 릴리스를 고치지 마십시오.**
- **경고(warning)는 서술합니다.** 센티널, 중복 수록된 판결, 본문만 있는 레코드,
  `core` 연계가 없는 레코드. 이것들이 바로 기여자가 처리 방식을 결정해야 할
  형태들입니다.

`select_index_inputs(records, evaluation_artifact_roots=...)`는 전달받은 자료와
임베딩 호출·인덱스에 도달하는 모든 것 사이의 **경계**입니다. `evaluation_only`이거나
`restricted`이거나 평가 산출물 트리에서 온 레코드가 있으면, 걸러 내지 않고
**예외를 던집니다.**

여기서 걸러 내는 것은 아무것도 안 하는 것보다 나쁩니다. 잘못 배선된 파이프라인이
**그럴듯한 수치를 계속 생산하고**, 거기서 보고되는 재현율은 누출을 측정한 값이 됩니다.
예외를 던지면 그 실수가 아직 귀속 가능한 지점에서 실행이 멈춥니다.

또한 스키마는 `restricted`이면서 `index_eligible`을 주장하는 자료를 **모델 생성
시점에** 거부하므로, 그 조합은 유효한 레코드로 존재할 수 없습니다.

누출 통제 두 가지는 **기여자의 몫**입니다. 소스 레코드가 아니라 청크 위에서
동작하기 때문입니다. 색인되는 모든 청크가 동결 매니페스트의 레코드에 속하는지
확인하고, 색인된 청크에 평가 질의나 기대 답변과 완전히 같거나 거의 같은 텍스트가
있는지 탐지하십시오. **질의 ID와 문서 ID가 교차하는지 보는 것은 이 일을 하지
못합니다** — 둘은 서로 다른 네임스페이스라, 누출이 남아 있는 채로 검사만 통과합니다.

## 확정된 범위 결정

- 주제가 이 에이전트의 질문에서 비켜난 레코드는 **릴리스에 남으며**
  `inDefaultCorpus: false`로 표시됩니다. 포함 여부는 위에서 설명한 대로 기여자의
  판단입니다.
- **추가로 수집하는 법령은 없습니다.** 대상 법령 범위는 닫혔습니다.
- **가이드 이미지는 릴리스에 전사되지 않습니다.** MZO가 OCR 솔루션을 제공하며,
  추출은 기여자의 작업입니다.

## 미결 항목

- **가이드 2건은 승인되었으나**, 이 스키마를 실측한 후보 JSONL에는 들어 있지
  않았습니다. `official_guide`는 승인된 원본 문서 자체에서 정의했으며, 전달될
  레코드가 여기에 부합할 것으로 봅니다.
- **평가 전용 자료**는 제한적 사용 조건 충족을 MZO가 확인한 뒤에야 릴리스에
  들어갑니다([DATASET.md](DATASET.md)). 어느 쪽이든 스키마는 준비되어 있습니다.
  `restricted` 반입 허용과 `evaluation_only` 사용 처분의 조합은 색인 가능한
  레코드로 생성될 수 없고, `select_index_inputs`가 이를 거부합니다.

둘 다 이 계약을 바꾸지 않습니다. 건수를 매니페스트에 위임한 것이 바로 그 때문입니다.

여기서 막히면 gyro에게 물으십시오. 무엇이 막혔는지, 어떤 해석이 가능한지, 해석에
따라 결과가 어떻게 달라지는지, 답이 없으면 어떤 가정으로 진행할지를 밝히십시오.
[README.md](README.md)를 참고하십시오.
