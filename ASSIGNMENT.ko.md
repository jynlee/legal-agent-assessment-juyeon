# 과제

> 한국어 번역본입니다. 정본은 [ASSIGNMENT.md](ASSIGNMENT.md)이며, 두 본이
> 어긋날 경우 영어 본문이 우선합니다.

## 목표

MZO가 제공하는 동결 법률 데이터셋으로부터, 재현 가능하고 근거에 기반한 General
Legal Agent 애플리케이션 서비스를 구축합니다. 개발자 한 명이 2주 안에 설계하고,
구현하고, 측정하고, 문서화할 수 있는 범위를 보여주는 것이 목적입니다.

## 수행할 작업

아래 전부가 기여자의 책임입니다.

1. 제공된 레코드를 분석하고, 각 문서 종류를 어떻게 사용할지 결정합니다.
2. 명시적이고 버전이 매겨진 규칙으로 코퍼스를 정규화하고 청킹합니다.
3. 버전이 매겨진 OpenSearch 3.5 인덱스를 재현 가능하게 생성합니다.
4. 질의 임베딩, 검색, 그리고 선택한 융합(fusion) 또는 리랭킹을 구현합니다.
5. 자신의 검색 성능 주장에 부합하는 테스트셋과 관련성 판정을 구축합니다.
6. Recall@k, MRR, nDCG 등 정량적 검색 지표를 정의하고 계산하며, 각 지표와
   컷오프가 왜 적절한지 설명합니다.
7. 고정된 Bedrock Claude 모델 정책을 통해 근거에 기반한 답변을 생성합니다.
8. 검증 가능한 인용과 함께, insufficient-evidence · out-of-scope ·
   dependency-unavailable 상태를 명시적으로 반환합니다.
9. 단일 턴, 무상태 애플리케이션 서비스 컨트랙트를 노출합니다.
10. 테스트, 재현 가능한 실행 명령, 아키텍처 결정, 한계, 투입 공수, 실제 소요
    시간, 비용 증거를 제출합니다.

## 고정 제약

- **데이터셋:** 동결된 MZO 릴리스만 사용합니다. 코퍼스 데이터 추가는 금지입니다.
- **검색:** AWS managed OpenSearch 3.5 호환.
- **임베딩:** AWS Bedrock Cohere Embed v4, 1536 차원.
- **생성:** `.env.example`에 게시된, Kit이 검증한 Bedrock Claude Sonnet 4.6 모델
  ID를 사용합니다.
- ingest와 query는 동일한 임베딩 모델, 차원, 전처리를 사용합니다.
- **공유 AWS 접근:** 기여자들은 IAM 사용자 하나, OpenSearch 도메인 하나, Bedrock
  쿼터 하나를 공유합니다. `legal-kit-assessment-<contributor>-...` 인덱스
  네임스페이스를 소유하고 그 안에만 머무르며, 관리형 도메인 요청은 전부 SigV4로
  서명하고, 임베딩 송신은 공유 분당 토큰 쿼터에 맞춰 속도를 조절합니다.
  `OPENSEARCH_ACCESS.md`를 보십시오.
- **작업 기간:** 착수 조건이 충족된 시점부터 2주.
- **런타임 경계:** 데이터베이스 없는 단일 턴 무상태 애플리케이션 서비스 하나이며,
  입출력은 타입이 정의되고 직렬화 가능해야 합니다.

정확한 Bedrock inference-profile ID, 프롬프트, 추론 파라미터,
데이터셋/청크/인덱스 버전, 토큰 수, 지연 시간, 추정 비용을 기록하십시오. 표시
이름(display name)에서 Opus나 Haiku의 모델 ID를 추측하지 마십시오. MZO는 하나의
동결된 Sonnet 구성으로 공식 비교를 수행하며, 이후 모든 활성 기여자에게 동등한
베이스라인 업데이트를 공지하는 경우에만 이 구성이 바뀝니다.

## 제공하지 않는 것

MZO는 다음을 규정하지 않습니다.

- 제공된 범위 내에서의 레코드 선택
- 정규화 및 청킹 전략
- 인덱스 매핑이나 벡터 엔진 파라미터
- dense, BM25, 하이브리드, 융합, 리랭킹 전략
- 테스트 질의, 관련성 판정, 지표, 컷오프, 합격 기준선
- 프롬프트, 답변 구성, 내부 프레임워크

**막히는 부분이 있으면 이 과제의 어느 부분이든 질문하십시오. 질문은 당연하며
감점 요소가 아닙니다.** 무엇을 모르는지 식별하고, 범위를 좁히고, 문장으로
정리하는 능력 자체가 MZO의 검토 대상이므로, **좋은 질문 하나가 조용한 가정보다
가치 있습니다.** MZO는 직접 답변하거나, 작은 공통 스키마 또는 예시를 모든 활성
기여자에게 동시에 공지합니다. 특정 기여자의 평가 결과를 바꾸는 개별 지원은
제공하지 않습니다. 제때 답이 올 수 없다면, 가정을 명시하고 그 위에서 진행하되,
질문과 가정을 모두 블로커 로그에 기록하십시오.

## 금지 사항

- 외부 코퍼스 레코드, 웹 검색 결과, 합성 법률 근거를 추가하는 것.
- 평가 질의, 기대 답변, 관련성 라벨을 인덱스에 넣는 것.
- 출처에서 파생된 근사 복제 검색 테스트를 실사용 품질인 것처럼 제시하는 것.
- 답변이 법률 자문이라거나, 어떤 행위가 확정적으로 적법하다고 주장하는 것.
- Peitho를 임포트하거나, Peitho DB 세션을 사용하거나, Peitho의 인증·테넌시·영속
  계층·DI·`ITool`·배포를 구현하는 것.
- 자격증명, 실제 PII, 제한된 원본 데이터, 생성된 데이터셋을 커밋하는 것.
- 자신의 기여자 네임스페이스 밖 인덱스를 읽거나 쓰거나 지우는 것, 그리고 클러스터
  전역 또는 와일드카드 파괴 연산을 호출하는 것. 자격증명이 공유되므로 그런 행위는
  **귀속도 복구도 불가능합니다.**

## 인수 경계

기여자가 보고한 지표는 증거이지 공식 인증 결과가 아닙니다. MZO는 동결 릴리스로부터
각 제출물을 재빌드하고 재실행합니다. **외부 인증 기관에 제출될 수 있는 것은
MZO가 재실행한 결과뿐입니다.** 제출했다고 해서 완성된 제출물 하나가, 또는 그
일부 구성요소가 채택되는 것이 보장되지는 않습니다.
